# =============================================================================
# Evella Admin (Event Horizon Dashboards) - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# All API requests use VITE_API_URL - ensure it points to your backend.
# =============================================================================

# -----------------------------------------------------------------------------
# Required: Backend API URL (all API requests go through this)
# -----------------------------------------------------------------------------
# Development (local backend):
VITE_API_URL=http://localhost:8000/api
# Production example:
# VITE_API_URL=https://api.validity.et/api

# -----------------------------------------------------------------------------
# Optional: API key for registration/login (if required by backend)
# -----------------------------------------------------------------------------
# VITE_API_KEY=your-api-key-here

# -----------------------------------------------------------------------------
# Optional: App URLs (for links, redirects)
# -----------------------------------------------------------------------------
# VITE_EVELLA_URL=http://localhost:3000
# VITE_PLATFORM_URL=http://localhost:5174

# -----------------------------------------------------------------------------
# Optional: Google Maps (for map components)
# -----------------------------------------------------------------------------
# VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# -----------------------------------------------------------------------------
# Optional: Messaging (polling / websocket)
# -----------------------------------------------------------------------------
# VITE_MESSAGING_TRANSPORT=websocket
# VITE_MESSAGING_POLL_INTERVAL_MS=3000
# VITE_MESSAGING_RESYNC_INTERVAL_MS=60000

# -----------------------------------------------------------------------------
# Optional: Pusher / Laravel Echo (realtime broadcasting)
# -----------------------------------------------------------------------------
# VITE_PUSHER_APP_KEY=your-pusher-key
# VITE_PUSHER_APP_CLUSTER=mt1
